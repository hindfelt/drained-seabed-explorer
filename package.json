{
  "name": "drained-seabed",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Interactive 3D visualization of a fully drained seabed — exposed reefs, shipwrecks, salt flats and canyons",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "generate": "node generator/cli.mjs",
    "preview": "vite preview",
    "test": "node --test 'generator/**/*.test.mjs'"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
